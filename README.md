# FinRep

## Dash MVP

The experimental Dash dashboard is developed in parallel with the existing Plotly report flow.
The old `main.py` report generation remains the fallback.

Run the local Dash app:

```bash
uv run python -m src.dashboard.app
```
